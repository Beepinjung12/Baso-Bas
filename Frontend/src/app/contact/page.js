import React from "react";
import Team from "@/app/components/contact/Team";

export const metadata = {
  title: "Contact | BASOBAS",
  description: "Get in touch with the BASOBAS team.",
};

const ContactPage = () => {
  return (
    <main>
      <Team />
    </main>
  );
};

export default ContactPage;
